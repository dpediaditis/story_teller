// TODO(B2): auth helpers for Edge Functions — verifying the Supabase user JWT
// for `auth: 'user'` endpoints, and the shared-secret header check for
// `auth: 'service'` endpoints (see contract.ts's `endpoints` registry, and
// WORKER_INTERNAL_SECRET in .env.example).

export {};
