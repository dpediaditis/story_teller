// TODO(B2): shared ApiResponse<T> envelope helpers (success/error) matching
// @papercub/shared's ApiResponse type, for every Edge Function in
// contract.ts's `endpoints` registry to return consistently.

export {};
