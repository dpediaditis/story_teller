// TODO(C1): worker entrypoint. Load config (src/config.ts), construct the
// provider bundle (src/providers/index.ts), start the queue consumer
// (src/queue.ts), and wire graceful shutdown.

export {};
